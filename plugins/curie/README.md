# Curie

The Curie plugin: one skill that installs the Curie CLI, signs you in, and then
follows the manual the CLI prints for itself.

Install it with your host's plugin command, or by copying this folder into
`~/.cursor/plugins/local/curie` for Cursor. The table is in the
[repository README](../../README.md).
